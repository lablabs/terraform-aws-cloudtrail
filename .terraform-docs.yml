formatter: markdown table

header-from: cloudtrail.tf
footer-from: docs/.footer.md

content: |-
  {{ include "docs/.intro.md" }}
  {{ .Header }}
  {{ .Requirements }}
  {{ .Providers }}
  {{ .Modules }}
  {{ .Resources }}
  {{ .Inputs }}
  {{ .Outputs }}
  {{ .Footer }}

output:
  file: README.md
  mode: replace
  template: |-
    <!-- BEGIN_TF_DOCS -->
    {{ .Content }}
    <!-- END_TF_DOCS -->
    {{- printf "\n" -}}

sections:
  hide:
    - providers

sort:
  by: required

settings:
  default: true
  required: true
